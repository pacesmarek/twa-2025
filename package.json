{
  "name": "ticket-system",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"vite\" \"sass --watch --poll src/scss/style.scss dist/style.css\"",
    "build": "vite build"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "sass": "^1.85.0",
    "vite": "^6.1.0"
  }
}
