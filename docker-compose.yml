version: '3.8'

services:
  app:
    image: php:8.2-apache
    container_name: php_app
    restart: always
    volumes:
      - C:/twa2025:/var/www/html
    ports:
      - "8080:80"
    environment:
      - APACHE_DOCUMENT_ROOT=/var/www/html/public
    networks:
      - app_network

  node:
    image: node:18
    container_name: node_service
    working_dir: /app
    volumes:
      - C:/twa2025:/app
      - C:/twa2025/node_modules:/app/node_modules
    command: tail -f /dev/null
    networks:
      - app_network

networks:
  app_network:
    driver: bridge
